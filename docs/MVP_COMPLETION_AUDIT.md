# ReviewFlow MVP completion audit

Date: 2026-09-03
Rule: fixture or local evidence never counts as real-platform acceptance.

## Requirement evidence

| Area | Required outcome | Current evidence | Status |
| --- | --- | --- | --- |
| Product scope | Local-first Windows desktop for individual creators; Xiaohongshu, Douyin, and Bilibili only | Electron/Vue workspace, FastAPI Sidecar, three-platform registry and capability endpoint | Locally verified |
| Content workflow | Master content, editable platform variants, scoring evidence, frozen interval predictions and benchmark fallback | `packages/domain`, immutable score cards, domain-filtered account/platform/kind history, prediction-owned content kind, empirical quantile ranges and observed-distribution bucket probabilities in versioned `prediction-v2`, platform variant editor, schema-validated BYOK scoring, CLI benchmark fallback, visible per-dimension evidence/suggestions and visible prediction ranges/baseline metadata | Locally verified |
| Renderer E2E | One video and one image/text item traverse score, prediction, preview, confirmation, publication, metric recovery, and retrospective | Nine real-Electron scenarios: three fast fixture-preload video/image-text/challenge paths, two production-lifecycle onboarding/restart phases, and four production-bridge phases for video, image/text, cold recovery, and native scheduling. Both production content paths traverse the complete workflow through the built production main/preload, random bearer-authenticated Sidecar, managed-media IPC, exact path policy, non-dry-run state machine, idempotent real SQLite job, operator confirmation, T+3 task, CSV/manual metrics, and six-metric retrospective; recovery reopens the persisted job, task, snapshot, retro, and media from separate desktop/Publisher SQLite stores. The native-schedule phase verifies manifest timestamps, uploader wall-clock arguments, Bilibili `tid`, platform-specific headed behavior, final-job command provenance, and retry idempotency. The zero-network adapter remains test-only, and the challenge path never synthesizes success | Locally verified; live platform response pending |
| Publish control | Immutable digest, exact summary, per-task confirmation, idempotency and `submitted/processing` not shown as success | Shared Python `PublishService`, SQLite-backed atomic job claim/listing, persisted desktop manifest and multi-job status recovery, interactive CLI and concurrent fixture tests | Locally verified |
| Adapter behavior | Capabilities, login/check, validation, preview, publish, local status and metric collection semantics | Complete adapter interface contract, `SauAdapter`, condition-aware account endpoints, streaming challenge/risk-control termination, non-synthetic success evidence, publication status endpoint and metric adapter/API composition; production IPC acceptance verifies headed Xiaohongshu/Douyin login commands, non-headed Bilibili login, and three unauthenticated checks without opening a terminal | Locally verified; live platform response pending |
| Scheduling | Use platform-native scheduling only and preserve local wall-clock semantics | Uploader commands use native `--schedule`; the timezone contract rejects naive timestamps, and the production-bridge E2E proves UI local time → offset-aware manifest → platform wall-clock command for scheduled targets while leaving an unscheduled target immediate | Locally verified |
| T+3 loop | Persist 72-hour work, resume on restart, retry safely, reject early snapshots, then require manual/CSV fallback | Versioned publisher SQLite queue, atomic claim leases with stale-worker token fencing, Electron-authorized task-list recovery, exact confirmed platform/reference checks for schedule and fetch, immutable task evidence, publication-linked snapshot validation, detached canonical actual-metric snapshots in TypeScript and Python reports, visible six-metric error comparison, three-attempt backoff, manual/CSV UI and scheduler tests. CSV import supports BOM, quoted commas/newlines and escaped quotes, rejects unclosed quotes, duplicate headers, inconsistent columns and files without supported metrics, and uniquely selects a publication from multi-row exports | Locally verified |
| Formula lifecycle | Ten same-context complete samples, experimental weights, non-regressing backtest and explicit activation | Domain-enforced unique one-to-one retro/sample IDs, account/platform/kind equality, complete samples, finite performance, tie-aware correlations, strict pairwise non-regression, visible old/new weights and correlations, explicit activation, and persisted experimental/active/retired version plus acceptance audit history | Locally verified |
| MVP metric | In 30 days, track at least eight confirmed publications and at least 80% completion among T+3-due work | Persisted publication records and the **今日** validation view | Instrumented; real 30-day outcome pending |
| Credentials | Renderer cannot retrieve stored secrets; Cookies/API keys encrypted at rest; temporary Sidecar credential exposure only | Electron `safeStorage`, a regression-tested public AI-config summary that omits the API Key, rollback-safe configuration/credential persistence, damaged-credential recovery, Windows DPAPI publisher sessions, and security tests; production-bridge checks prove missing/wrong bearer rejection (401), disallowed Origin rejection (403), and fixture data containment before the Sidecar app is imported | Locally verified |
| Local data | Versioned SQLite business state, application-managed media, portable import/export and redacted diagnostics | Transactional desktop `PRAGMA user_version` migration registry with compatible v0 payload preservation, required-schema validation, rollback without version advance, and future-version rejection; transactional publisher v1→v2→v3→v4 migrations in a separate `publisher-data` SQLite store; production-bridge complete-flow and cold-restart recovery; managed media plus workspace bundle round-trip/integrity tests; and exact diagnostic-allowlist regression test | Locally verified |
| CLI | `account`, `content`, `publish`, and `retro` command groups with interactive execute confirmation | All five CLI help probes exit 0; publish CLI contract tests; account child-process output redaction test; structured prediction applies the desktop context/deduplication/six-metric contract while preserving legacy compact input; retrospective requires the confirmed publication ID and rejects a mismatched snapshot | Locally verified |
| Windows distribution | No system Python required; embedded Sidecar/uploader; clean installation path works without orphan runtimes | Guarded `smoke-installed-release.ps1` temporary install/uninstall, minimal-`PATH` production Sidecar startup, real-publishing default check, forced-parent-exit lifecycle check, and an installed-UI complete-flow plus cold-restart recovery using a separately packaged Sidecar replacement that rejects every non-loopback DNS lookup and socket connection. The workflow verifies managed media, separate desktop/Publisher SQLite stores, non-dry-run idempotent job, operator confirmation, T+3 manual metrics, retrospective, and recovery from the installed artifact; production Sidecar/uploader smoke tests also pass. [Hosted CI 33658717922](https://github.com/Jason-zzr/ReviewFlow/actions/runs/33658717922) passed on commit `6a96eb3` and published a 359,384,634-byte artifact with GitHub digest `c4a1dd8a...adcf43`; the current unpushed candidate still requires its own hosted run | Locally verified on current Windows host; each pushed release candidate requires matching hosted evidence |
| Licensing | MIT project license and pinned upstream provenance/licenses | `THIRD_PARTY_NOTICES.md` and `licenses/` | Verified |
| Real publishing | Human-authorized login, exact account/content/time, real post verification and external reference | No authorization record supplied | Pending external gate |
| Real T+3 evidence | Collect or manually import the actual platform result after 72 hours and complete a retrospective | Requires a real confirmed publication and elapsed T+3 | Pending external gate |

## Latest local acceptance

- Domain: 39 tests passed, including immutable score snapshots, prediction-owned content kind, validated freeze timestamps, contextual history filtering, empirical quantile intervals, sample-derived bucket probabilities, versioned prediction rationale, publication-linked retrospectives with detached actual metrics, unique same-context formula samples, immutable experiment records, auditable version activation, tie-aware correlations, and strict pairwise non-regression.
- Electron: 33 tests passed, including BYOK key isolation, rollback-safe configuration/credential persistence, damaged-credential recovery, portable workspace media round-trip, explicit transactional v0 migration, strict legacy column-shape validation and failure rollback, unfinished-onboarding recovery, diagnostic allowlisting, exact Sidecar recovery-path policy including confirmed publication IDs, multi-job recovery, content-kind-aware frozen publication-context validation, JSON-compatible detachment of Vue reactive bridge payloads, and six quote-aware CSV contract tests.
- Renderer E2E: 9 real-Electron local scenarios passed: three fast fixture-preload video/image-text/challenge paths, two production-lifecycle phases, and four production-bridge video/image-text/recovery/native-schedule phases. The production bridge uses the built main/preload, random bearer session, exact IPC allowlist, managed media, separate real SQLite stores, non-dry-run idempotent jobs, CSV/manual T+3 metrics, persisted retrospectives, native schedule and command-provenance assertions, three account-command shapes, and negative auth/origin checks. It uses no user credentials or platform network requests and therefore remains local evidence only.
- Publisher: 94 tests passed, including explicit authenticated adapter-factory injection, detached retrospective actual-metric snapshots, the complete adapter contract, immediate challenge/risk-control process termination, UTF-8 child-process handling, non-synthetic success evidence, retained preview-command provenance in terminal and partial multi-platform job details, conservative partial-submission status and idempotent retry behavior, condition-aware account checks, desktop-parity empirical prediction ranges/buckets/protocol metadata, CLI retrospective publication linkage, forced concurrent publish/metric races, stale-worker-fenced claim-lease recovery, manifest timezone validation, multi-video rejection, confirmed-publication metric linkage, evidence-bound fetch/schedule behavior, terminal-task immutability, and persisted job/task listings; one non-blocking Starlette/httpx deprecation warning.
- Installer: `apps/desktop/release/ReviewFlow-0.1.0-x64-setup.exe`.
- Installer SHA-256: `b77bc40d19ffac3dd4a7f739fbaa1bccaf72e739a1c9aac459d84eae835d8f88`.
- Installed-package acceptance: restricted-`PATH` production startup and parent-watchdog shutdown passed; a temporary-install-only Sidecar replacement with enforced loopback-only networking then completed score/predict/confirm/publish/metrics/retro and cold-restart recovery before silent uninstall.

## Remaining authorization record

Before a real run, record the platform, exact account alias, title, managed media filenames, immediate/native schedule time, manifest digest, idempotency key, operator timestamp, and whether the post may remain public. Any manifest change invalidates that authorization.
