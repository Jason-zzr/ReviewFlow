# ReviewFlow MVP local acceptance report

Date: 2026-09-03
Scope: Windows local build, fixture-based platform acceptance, isolated production-bridge lifecycle/workflow acceptance, and temporary installed-package complete-flow acceptance

## Result

The local MVP implementation is buildable and its Windows x64 installer is available at `apps/desktop/release/ReviewFlow-0.1.0-x64-setup.exe`.

- TypeScript domain tests: 40 passed, including immutable score-card snapshots, prediction-owned content kind, validated freeze timestamps, deep-frozen prediction restoration after JSON persistence, domain-filtered contextual prediction history, empirical P10/P50/P90 ranges, observed bucket distributions, versioned prediction rationale, retrospective publication linkage and detached actual-metric snapshots, complete and uniquely linked formula calibration samples, immutable experiment records, auditable rubric activation/version history, tie-aware correlations, strict pairwise non-regression, the 30-day publication target, and T+3 capture-time contracts.
- Electron tests: 33 passed for BYOK key isolation, transactional configuration/credential persistence, damaged-credential recovery, managed media boundaries, portable workspace bundles, workspace persistence, explicit version-zero migration with legacy-payload preservation, strict legacy column-shape validation, rollback on incompatible legacy schemas, future-version rejection, unfinished onboarding recovery, exact diagnostic-export allowlisting, Sidecar recovery-path policy including confirmed `jobId:platform` latest-snapshot IDs, multi-job recovery, content-kind-aware frozen publication-context selection, bridge-payload detachment from Vue reactive proxies, and quote-aware metrics CSV parsing with structural validation.
- Real-Electron E2E: 9 local scenarios passed. Three fast fixture-preload scenarios cover video, image/text, and platform-challenge handling; their complete flows verify visible score evidence, versioned prediction ranges, immutable preview, per-task confirmation, idempotency, explicit publication confirmation, CSV/manual six-metric recovery, and retrospective generation, while the challenge path stops at `userActionRequired`. Two production-lifecycle phases launch the built production main/preload in separate processes and prove onboarding plus edited content survive a restart. Four production-bridge phases (`production_video`, `production_image_text`, `production_recover`, and `production_schedule`) exercise the complete score → prediction → preview → non-dry-run publish → operator confirmation → T+3 metric import → retrospective path through the production main/preload, random bearer-authenticated localhost Sidecar, managed media import, exact IPC allowlist, and separate real desktop/Publisher SQLite stores; the restart phase recovers the persisted job, task, snapshot, retro, and media. The schedule phase proves UI local-time input becomes an offset-aware immutable manifest value and the intended native `--schedule` wall-clock argument for Xiaohongshu and Bilibili, while Douyin remains immediate; it also verifies Bilibili `tid`, platform-specific headed behavior, retained command provenance after the uploader exits, and one job after an idempotent retry. The production bridge also verifies missing/wrong bearer responses (401), disallowed Origin response (403), all three platform login-command shapes, and the three no-credential account-check messages. Its adapter is test-only and zero-network. No scenario opens a login terminal, reads user credentials, performs a platform request, or claims live publishing success.
- Python publisher tests: 96 passed, including explicit authenticated adapter-factory injection, detached retrospective actual-metric snapshots, sequential/partial/future-version migration behavior, the complete platform-adapter contract, immediate challenge/risk-control process termination, UTF-8 child-process handling, non-synthetic uploader success, condition-aware account checks, desktop-parity empirical prediction ranges/buckets/protocol metadata, CLI retrospective publication linkage, per-target CLI publish confirmation with zero-side-effect rejection, manifest timezone enforcement, multi-video rejection, confirmed-publication metric and evidence linkage, immutable terminal metric tasks, atomic publish and token-fenced metric-task claims, retained commands and conservative status after a partial multi-platform submission, persisted job/task listing, account-command output redaction, and Windows parent-process lifecycle handling.
- npm audit: 0 vulnerabilities at the configured high threshold.
- Python dependency check: no broken requirements; pinned publisher dependencies verified.
- Packaged Sidecar: healthy; unauthenticated API request returned 401.
- Packaged publisher runtime: offline doctor passed for Patchright, the system browser, and Biliup 1.2.4.
- Xiaohongshu, Douyin, and Bilibili packaged account checks all returned `account_auth_required` with exit code 20 when no credentials were present.
- Packaged desktop startup: Electron and its embedded Sidecar remained running during the startup probe; managed media remained present across startup and clean user data contained no real-publishing opt-in setting.
- The reusable `scripts/smoke-installed-release.ps1` check installed the NSIS package into a validated temporary directory with a minimal system `PATH`; the unmodified installed desktop and both PyInstaller Sidecar process levels stayed alive, real publishing remained disabled, and force-ending Electron released the production Sidecar. It then replaced the Sidecar only inside that temporary installation with a separately packaged fixture that rejects DNS resolution and socket connections for every non-loopback host, drove the installed UI through score → prediction → exact confirmation → non-dry-run publish state machine → operator confirmation → T+3 manual metrics → retrospective, and cold-restarted the installed app to recover the same job, task, snapshot, retro, managed media, and separate SQLite stores. The silent uninstaller and guarded cleanup completed successfully.
- The desktop **今日** view was rendered and visually checked at 1440 × 920 with the persisted MVP validation tracker, account-scoped rubric progress, and formula version history visible.
- [GitHub-hosted CI 33675398240](https://github.com/Jason-zzr/ReviewFlow/actions/runs/33675398240) completed successfully for commit `d30de1c`. Its `verify` and `windows-release` jobs passed and produced the 359,389,717-byte `reviewflow-windows-d30de1cc4ebb49f54766ff077ee4a6e7518a6750` artifact with GitHub digest `sha256:864f611fc48d14d0114893a7e0437da65f8773bd092eceee66a5a877bc4c4fb0`; every pushed release candidate still requires a matching hosted run.

## Release artifact

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `ReviewFlow-0.1.0-x64-setup.exe` | 234,915,488 | `0c19a8f05090ea7083cdcd65af59f1f64c1cb1303ba3eaab38dfb3a3b228dfff` |
| `reviewflow-sidecar.exe` | 14,593,376 | `264165f62d6e6a778d5f23c1bfee9beb700e1bd66e5081ea3f17634c1e0fb80f` |
| `reviewflow-sau.exe` | 99,782,830 | `f710102d84626b7b66340e5bc255eb4f56137668670c1d28540f05c032c86d26` |
| `biliup.exe` | 33,895,936 | `5500912978355e7a64dbbe86ebd7ade2cd4ec8bbc91e16998e4aea7359d0fcdd` |

## Outstanding acceptance gates

The local suites cover the domain, real Electron renderer, production Electron main/preload, authenticated Sidecar API, adapters, CLI, persistence, installed-package startup/lifecycle, and an installed-package complete workflow through a test-only zero-network Sidecar replacement. Platform outcomes still use test-only adapters or offline checks; none of this establishes real-platform behavior.

No real platform login, public post, native scheduled post, or live metric collection was performed. Those checks require a fresh authorization record for the exact account, content, schedule, manifest digest, and idempotency key described in `REAL_PLATFORM_ACCEPTANCE.md`. Fixture and local package evidence must not be presented as real-platform acceptance.
